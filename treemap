install.packages("treemap")  # If not already installed
install.packages("readr")    # If not already installed
library(treemap)
library(readr)
visa_data <- read_csv("data/visa_distribution_final-total.csv")

str(visa_data)
head(visa_data)

